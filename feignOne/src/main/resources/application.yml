eureka:
  instance:
    prefer-ip-address: true
  client:
    serviceUrl:
      defaultZone: http://192.168.31.127:8761/eureka/
server:
  port: 8766
spring:
  application:
    name: feign-one
feign:
  hystrix:
    enabled: true